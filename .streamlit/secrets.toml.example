# Copy this file's contents into the Streamlit Cloud app Secrets panel.
# Do NOT rename this file to secrets.toml — it is intentionally excluded from git.
ANTHROPIC_API_KEY = "your_key_here"

# Optional: enables live web search in the Market Researcher agent.
# Get a key at https://exa.ai
# When not set, the app falls back to AI-only market analysis (no search).
EXA_API_KEY = ""
